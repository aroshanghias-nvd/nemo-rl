# GRPO Algorithm Configuration (InternVL3.5-4B Cascade RL replica)
# Mirrors key settings from InternVL's cascade RL script
# Ref: https://github.com/Weiyun1025/verl-internvl/blob/main/shell/internvl3_5_4b.sh

grpo:
  # 512 prompts × 16 generations = 8192 trajectories (matches VERL data.train_batch_size=512)
  num_prompts_per_step: 512
  num_generations_per_prompt: 16
  max_rollout_turns: 1
  max_num_epochs: 1
  max_num_steps: 1000000
  normalize_rewards: true
  use_leave_one_out_baseline: false
  val_period: 10
  val_at_start: false 
  overlong_filtering: true
  max_val_samples: 256
  val_batch_size: 256
  seed: 42

loss_fn:
  # GSPO-style sequence-level formulation (as in VERL)
  reference_policy_kl_penalty: 0.0
  ratio_clip_min: 0.2
  ratio_clip_max: 0.2
  ratio_clip_c: null
  use_on_policy_kl_approximation: false
  use_importance_sampling_correction: false
  sequence_level_importance_ratios: true
  token_level_loss: false

checkpointing:
  enabled: true
  checkpoint_dir: "results/grpo_internvl3_5_4b_cascade_rl"
  metric_name: "val_reward"
  higher_is_better: true
  keep_top_k: 50
  save_period: 10
  checkpoint_must_save_by: "00:03:45:00"

policy:
  model_name: "OpenGVLab/InternVL3_5-4B-MPO"
  tokenizer:
    name: ${policy.model_name}
    # https://huggingface.co/OpenGVLab/InternVL3_5-4B-HF/blob/main/chat_template.jinja
    chat_template: "{% for message in messages %}{{'<|im_start|>' + message['role'] + '\n'}}{% if message['content'] is string %}{{ message['content'] }}{% else %}{% for content in message['content'] %}{% if content['type'] == 'image' %}{{ '<image>\n' }}{% elif content['type'] == 'video' %}{{ '<video>\n' }}{% elif content['type'] == 'text' %}{{ content['text'] }}{% endif %}{% endfor %}{% endif %}{{'<|im_end|>\n'}}{% endfor %}{% if add_generation_prompt %}{{'<|im_start|>assistant\n' }}{% endif %}"

  # VERL GSPO mini-batch and micro-batch settings
  # train_global_batch_size = PPO_MINI_BATCH_SIZE (32 prompts) × N_SAMPLES_PER_PROMPT (16) = 512 trajectories
  # Perfectly divides across 64 GPUs: 512 / 8 = 64
  train_global_batch_size: 512
  train_micro_batch_size: 8
  generation_batch_size: 8
  logprob_batch_size: 8

  # Sequence length and precision
  max_total_sequence_length: 16384
  precision: "bfloat16"

  dtensor_cfg:
    # _v2: true
    enabled: true
    cpu_offload: True  # Memory optimization
    sequence_parallel: false  # NOT SUPPORTED for VLM models
    activation_checkpointing: true
    tensor_parallel_size: 1
    context_parallel_size: 1  # MUST be 1 for VLM models
    custom_parallel_plan: null
    clear_cache_every_n_steps: 10

  # Dynamic batching token budgets aligned with VERL
  dynamic_batching:
    enabled: True
    train_mb_tokens: 16384
    logprob_mb_tokens: 16384
    sequence_length_round: 64

  make_sequence_length_divisible_by: ${policy.dtensor_cfg.tensor_parallel_size}
  max_grad_norm: 1.0

  sequence_packing:
    enabled: False

  optimizer:
    name: "torch.optim.AdamW"
    kwargs:
      lr: 1e-6
      weight_decay: 0.01
      betas: [0.9, 0.999]
      eps: 1e-8
      foreach: False
      fused: False

  scheduler:
    name: "torch.optim.lr_scheduler.ConstantLR"
    kwargs:
      factor: 1.0
      total_iters: 10000000000

  generation:
    backend: "vllm"
    # Sampling aligned with VERL GSPO
    max_new_tokens: 6144
    temperature: 1.0
    top_p: 1.0
    top_k: null
    stop_token_ids: null
    stop_strings: ["</answer>"]
    vllm_cfg:
      async_engine: false
      precision: ${policy.precision}
      tensor_parallel_size: 1
      pipeline_parallel_size: 1
      expert_parallel_size: 1
      gpu_memory_utilization: 0.85
      max_model_len: ${policy.max_total_sequence_length}
      enforce_eager: False
      skip_tokenizer_init: true
      limit_mm_per_prompt: {"image": 12}
    colocated:
      enabled: true
      resources:
        gpus_per_node: null
        num_nodes: null

data:
  max_input_seq_length: ${policy.max_total_sequence_length}
  prompt_file: null
  system_prompt_file: examples/prompts/internvl_r1_system_prompt.txt
  dataset_name: "mmpr_tiny"
  split: "train"
  shuffle: true
  val_size: 500
  cache_dir: "/lustre/fsw/portfolios/llmservice/users/ikarmanov/rl_datasets/mmpr_tiny_images"

env:
  mmpr_tiny:
    num_workers: 8
    reward_functions:
      - name: verl_geo3k
        weight: 1.0
        kwargs:
          format_score: 0.1

logger:
  log_dir: "logs"
  num_val_samples_to_print: 0
  wandb_enabled: false
  tensorboard_enabled: true
  mlflow_enabled: false
  swanlab_enabled: false
  monitor_gpus: true
  wandb:
    project: "grpo-dev"
    name: "grpo-internvl3_5-4b-cascade-rl"
  tensorboard: {}
  gpu_monitoring:
    collection_interval: 10
    flush_interval: 10

cluster:
  gpus_per_node: 8
  num_nodes: 1 # Run on 8 nodes


